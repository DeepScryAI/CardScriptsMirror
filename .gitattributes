# Resource files are mirrored byte-for-byte from upstream. Do not let git
# normalize line endings or otherwise rewrite their content, so the mirror
# stays an exact copy of Card-Forge/forge.
cardsfolder/**   -text
tokenscripts/**  -text
puzzle/**        -text
tutorial/**      -text
